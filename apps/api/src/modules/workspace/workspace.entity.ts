import { Field, ObjectType } from '@nestjs/graphql';
import { Column, Entity } from 'typeorm';
import { NodeColumns } from '../abstracts/node.columns';

@ObjectType()
@Entity('workspaces')
export class WorkspaceEntity extends NodeColumns {
  @Field()
  @Column()
  name: string;

  

  @Field()
  @Column('uuid', { name: 'firm_id' })
  firmId: string;
}
